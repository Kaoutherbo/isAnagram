#ifndef MAIN_H
#define MAIN_H
// Header files
#include <stdio.h>
#include <stdlib.h>
#include<string.h>
#include <stdbool.h>
// Prototypes for every function in the project
char* getString(char*);
bool isAnagram(char*,char*);

#endif